import React from "react";

import { ScrollViewSheet, SectionHeader } from "!/c/misc/sheet-utils";

export const CreateEventFromPreviousSheet = () => {
  return (
    <ScrollViewSheet>
      <SectionHeader text="Tap to select the event you want to copy and create a new event from" />
    </ScrollViewSheet>
  );
};
