{
  "name": "@vibefire/models",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "update": "pnpm update",
    "update-latest": "pnpm update --latest"
  },
  "devDependencies": {
    "@vibefire/config": "workspace:*"
  }
}
